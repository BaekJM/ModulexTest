import 'package:flutter/material.dart';

class Goods_Today extends StatelessWidget {
  const Goods_Today({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('50'),
    );
  }
}
